import { createFileRoute } from "@tanstack/react-router";
import { MessageCircle, Phone } from "lucide-react";
import hero from "@/assets/rufiji-sunrise.jpg";
import { Button } from "@/components/ui/button";
import { PageHero } from "@/components/safari/content";
import { whatsappUrl } from "@/components/safari/data";
export const Route = createFileRoute("/contact")({ head: () => ({ meta: [{ title: "Plan Your Tanzania Safari — Dozzo Safari" }, { name: "description", content: "Contact Dozzo Safari to start planning a personalized Tanzania safari." }, { property: "og:title", content: "Plan Your Tanzania Safari — Dozzo Safari" }, { property: "og:description", content: "Start a conversation about your Tanzania safari." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: ContactPage });
function ContactPage() { return <><PageHero eyebrow="Start planning" title="Let’s talk about your Tanzania." text="Share your travel dates, group size and the parks on your wish list. We’ll help you shape the right journey." image={hero} /><section className="section-space bg-background"><div className="site-container grid gap-6 md:grid-cols-2"><a href={whatsappUrl} target="_blank" rel="noreferrer" className="contact-panel group"><MessageCircle className="size-7 text-primary" /><p className="mt-12 text-xs font-bold uppercase text-primary">Fastest response</p><h2 className="mt-3 font-display text-3xl font-extrabold">WhatsApp Dozzo</h2><p className="mt-3 text-muted-foreground">Start a direct conversation with our Tanzania team.</p><Button className="mt-7">Open WhatsApp</Button></a><a href="tel:+255713956532" className="contact-panel group"><Phone className="size-7 text-primary" /><p className="mt-12 text-xs font-bold uppercase text-primary">Call us</p><h2 className="mt-3 font-display text-3xl font-extrabold">+255 713 956 532</h2><p className="mt-3 text-muted-foreground">Speak directly with Dozzo Safari.</p><Button variant="dark" className="mt-7">Call now</Button></a></div></section></>; }