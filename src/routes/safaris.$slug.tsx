import { createFileRoute, Link, notFound } from "@tanstack/react-router";
import { ArrowLeft, Clock, MapPin, MessageCircle } from "lucide-react";
import { Button } from "@/components/ui/button";
import { packages, whatsappUrl } from "@/components/safari/data";
export const Route = createFileRoute("/safaris/$slug")({ loader: ({ params }) => { const safari = packages.find((item) => item.slug === params.slug); if (!safari) throw notFound(); return safari; }, head: ({ loaderData }) => ({ meta: [{ title: loaderData ? `${loaderData.name} — Dozzo Safari` : "Safari Not Found — Dozzo Safari" }, { name: "description", content: loaderData?.description ?? "Explore Tanzania safaris with Dozzo Safari." }, { property: "og:title", content: loaderData ? `${loaderData.name} — Dozzo Safari` : "Tanzania Safaris — Dozzo Safari" }, { property: "og:description", content: loaderData?.description ?? "Explore Tanzania safaris with Dozzo Safari." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: SafariDetail });
function SafariDetail() { const safari = Route.useLoaderData(); return <><section className="relative isolate min-h-[62vh] bg-header"><img src={safari.image} alt={safari.destination} className="absolute inset-0 size-full object-cover" width={1408} height={1008} /><div className="absolute inset-0 bg-hero-shade" /><div className="site-container relative flex min-h-[62vh] items-end pb-16 pt-28"><div><Link to="/safaris" className="mb-8 inline-flex items-center gap-2 text-sm font-semibold text-hero-muted hover:text-hero-foreground"><ArrowLeft className="size-4" /> All safaris</Link><h1 className="page-title text-hero-foreground">{safari.name}</h1><div className="mt-5 flex flex-wrap gap-5 text-sm text-hero-muted"><span className="inline-flex items-center gap-2"><MapPin className="size-4 text-primary" />{safari.destination}</span><span className="inline-flex items-center gap-2"><Clock className="size-4 text-primary" />{safari.duration}</span></div></div></div></section><section className="section-space bg-background"><div className="site-container grid gap-12 lg:grid-cols-[1.5fr_0.75fr]"><div><p className="eyebrow"><span />The journey</p><h2 className="section-title mt-5">A closer look at {safari.destination}.</h2><p className="mt-6 max-w-3xl text-lg leading-9 text-muted-foreground">{safari.description} Your final route, accommodation and timing will be planned around your travel dates and preferences.</p></div><aside className="border-t-2 border-primary bg-warm p-7"><p className="text-xs font-bold uppercase text-primary">Plan this safari</p><p className="mt-4 text-sm leading-7 text-muted-foreground">Tell us your dates and group size for a personalised itinerary and confirmed price.</p><Button asChild size="xl" className="mt-6 w-full"><a href={`${whatsappUrl}%20I%27m%20interested%20in%20${encodeURIComponent(safari.name)}.`} target="_blank" rel="noreferrer"><MessageCircle /> WhatsApp Dozzo</a></Button></aside></div></section></>; }